stages:
  - tests

tests:
  image: 'python:3.11.7-bullseye'
  stage: tests

  variables:
    GIT_DEPTH: 1
  script:
    - apt-get install graphviz
    - python3 -m venv venv
    - source venv/bin/activate
    - pip install --upgrade wheel setuptools pip
    - pip install cython
    - python3 -m pip install -r requirements.txt
    - git submodule sync
    - git submodule update --init --recursive --depth=1 -- submodules/dag-flow
    - PYTHONPATH="$PYTHONPATH:/builds/dag-computing/dagflow-detector/submodules/dag-flow"
    - mkdir output
    - coverage run --source=. --omit=submodules/* -m pytest
    - coverage report
    - coverage xml
  coverage: '/(?i)total.*? (100(?:\.0+)?\%|[1-9]?\d(?:\.\d+)?\%)$/'
  artifacts:
    paths:
      - test
      - output
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml
  only:
    - master
    - main
    - merge_requests
